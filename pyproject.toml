[project]
name = "git-and-ssh"
version = "1.0.0"
description = "A simple guide to using Git and SSH"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mkdocs-material==9.7.6",
    "zensical==0.0.33",
]

[dependency-groups]
dev = [
    "pre-commit>=4.5.1",
]
